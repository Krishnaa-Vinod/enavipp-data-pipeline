# Dataset spec (draft)

This repo will ultimately build NoMaD-style training tuples in event space:
- Past observations: P event voxel grids
- Optional goal voxel grid + goal mask
- Predict: future action sequence + distance-to-goal

(Keep this doc updated as the preprocessing design stabilizes.)
